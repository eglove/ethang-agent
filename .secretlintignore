# Intentionally secret-SHAPED test fixtures: these strings must match real
# credential patterns for SkillContentScanner's detection tests to work, so
# secretlint's findings here are expected false positives by design.
tests/eThangAgent.Skill.Domain.Tests/SkillContentScannerTests.cs
