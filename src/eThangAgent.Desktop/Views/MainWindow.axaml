<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:eThangAgent.Desktop.ViewModels"
        x:Class="eThangAgent.Desktop.Views.MainWindow"
        Title="eThang Agent" Width="900" Height="650"
        Closed="OnWindowClosed">
  <DockPanel>
    <!-- Status bar: phase, model id, message count (parity with the terminal StatusLine). -->
    <Border DockPanel.Dock="Bottom" Background="#22000000">
      <StackPanel Orientation="Horizontal" Spacing="12" Margin="8,4">
        <TextBlock x:Name="SpinnerText" Text="{Binding Status.Phase}"/>
        <TextBlock Text="{Binding Status.ModelId}"/>
        <TextBlock Text="{Binding MessageCount, StringFormat='messages: {0}'}"/>
      </StackPanel>
    </Border>

    <!-- Input area OR clarify mode. -->
    <DockPanel DockPanel.Dock="Bottom">
      <!-- Clarify mode: replaces the input row while a question is pending. -->
      <StackPanel x:Name="ClarifyArea" DockPanel.Dock="Bottom"
                  IsVisible="{Binding Clarify, Converter={x:Static ObjectConverters.IsNotNull}}">
        <TextBlock Text="{Binding Clarify.Question}" TextWrapping="Wrap" Margin="8,4"/>
        <TextBlock Text="{Binding Clarify.ValidationMessage}" Foreground="OrangeRed" Margin="8,0"
                   TextWrapping="Wrap"
                   IsVisible="{Binding Clarify.ValidationMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
        <ItemsControl ItemsSource="{Binding Clarify.Options}" Margin="8,2">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Button Content="{Binding}" Click="OnClarifyOption" Margin="2"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Horizontal" Spacing="8" Margin="8,4"
                    IsVisible="{Binding Clarify.AllowFreeText}">
          <TextBox x:Name="ClarifyInput" Text="{Binding Clarify.Input}" Width="480"
                   KeyDown="OnClarifyInputKeyDown"/>
          <Button Content="Answer" Click="OnClarifyAnswer"/>
          <Button Content="Cancel" Click="OnClarifyCancel"/>
        </StackPanel>
      </StackPanel>

      <!-- Normal input row with command autocomplete. -->
      <StackPanel x:Name="InputArea" DockPanel.Dock="Bottom">
        <TextBox x:Name="InputBox" Watermark="Type a message. /help for commands"
                 AcceptsReturn="True" TextWrapping="Wrap" MaxHeight="120"
                 IsEnabled="{Binding !IsBusy}"/>
        <Popup x:Name="CommandPopup" PlacementTarget="{Binding #InputBox}" PlacementMode="Top"
               IsLightDismissEnabled="False">
          <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Background="#FF1E1E1E">
            <ListBox x:Name="CommandList" MaxHeight="120" MinWidth="320"
                     DoubleTapped="OnCommandChosen" KeyDown="OnCommandListKeyDown"/>
          </Border>
        </Popup>
      </StackPanel>
    </DockPanel>

    <!-- Transcript: one implicit template per entry type. -->
    <ScrollViewer x:Name="TranscriptScroll">
      <ItemsControl x:Name="TranscriptList" ItemsSource="{Binding Transcript.Entries}" Margin="4,8">
        <ItemsControl.DataTemplates>
          <DataTemplate DataType="vm:UserMessageEntry">
            <TextBlock Text="{Binding Text}" FontWeight="Bold" Margin="8,2" TextWrapping="Wrap"/>
          </DataTemplate>
          <DataTemplate DataType="vm:AssistantTextEntry">
            <TextBlock Text="{Binding Text}" Margin="8,2" TextWrapping="Wrap"/>
          </DataTemplate>
          <DataTemplate DataType="vm:ReasoningEntry">
            <TextBlock Text="{Binding Text}" FontStyle="Italic" Opacity="0.7" Margin="8,2" TextWrapping="Wrap"/>
          </DataTemplate>
          <DataTemplate DataType="vm:ToolCallEntry">
            <TextBlock Margin="8,2" TextWrapping="Wrap">
              <Run Text="⚙ "/><Run Text="{Binding Name, Mode=OneWay}"/><Run Text=" "/><Run Foreground="Gray" Text="{Binding Arguments, Mode=OneWay}"/>
            </TextBlock>
          </DataTemplate>
          <DataTemplate DataType="vm:ToolResultEntry">
            <TextBlock Margin="8,2" TextWrapping="Wrap">
              <Run Text="↳ "/><Run Text="{Binding Name, Mode=OneWay}"/><Run Text=" "/><Run Text="{Binding Summary, Mode=OneWay}"/>
            </TextBlock>
          </DataTemplate>
          <DataTemplate DataType="vm:NoticeEntry">
            <TextBlock Text="{Binding Text}" Foreground="DarkGray" Margin="8,2" TextWrapping="Wrap"/>
          </DataTemplate>
        </ItemsControl.DataTemplates>
      </ItemsControl>
    </ScrollViewer>
  </DockPanel>
</Window>